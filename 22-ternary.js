//Ternary if Statement

var number = 11;

//using a ternary operator
/**
 *  -the ternary operator below can be explained by
 *  - if we divide the value(number) by 2, do we get a remainder of 0, if we do , then the reult is
 *    even or odd, then store the result in a variable called "result".
 */
var result = number % 2 == 0 ? "Even" : "Odd";
/*using if statement to find odd or even numbers.
if (number % 2 == 0) {
	console.log("the number is even");
} else {
	console.log("the number is even");
}
  */
