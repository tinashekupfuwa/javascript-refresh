//Strings

/**
 * A string is a datatype that allows you to store a sequence of characters.
 */
var modeOfTransport = "car";

console.log(typeof modeOfTransport);
console.log(modeOfTransport.length);
console.log(modeOfTransport.toUpperCase());
console.log(modeOfTransport.substring(0, 5));

var carBrand = "Mazda";
var model = "626";

console.log(carBrand + " " + model);
console.log(`${carBrand} ${model}`);
