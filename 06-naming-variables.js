//Naming variables

/**
 * Variable names
 * -cannot contain spaces
 * -must begin with a letter,
 *  an underscore (_) or a dollar sign ($).
 * -names can only contain letters,
 *    numbers, underscores or dollar signs.
 * -names are case sensitive.
 * -cannot use reserved keywords.
 *
 * --------------------------------------
 * use camelCase when naming variables.
 * -variables need to have a meaning that resembles the value.
 * -for Booleans try use "is",an example "isRaining,isMarried,isFemale"
 */
